$VLESS_DOMAIN {
        bind 0.0.0.0
        reverse_proxy * 127.0.0.1:8000

        header {
                -Server
        }
        handle_errors {
                header {
                        -Server
                }
        }
}
